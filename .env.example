# NguonC Stremio Addon - Environment Configuration

# Port để chạy server (mặc định: 7000)
PORT=7000

# Host binding (0.0.0.0 = tất cả interface, 127.0.0.1 = chỉ localhost)
HOST=0.0.0.0

# NODE_ENV: development | production
NODE_ENV=development
